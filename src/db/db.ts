// import Logger from '../utils/logger';

// let dbConnection: typeof mongoose;

// const getConnection = async (): Promise<typeof mongoose> => {
//   if (!dbConnection) {
//     Logger.error('DB connection is not initialized');
//     try {
//       dbConnection = await mongoose.connect(dbUrl, mongoOptions);
//       Logger.info('DB connection initialized');
//     } catch (error) {
//       throw new Error(error);
//     }
//   }
//   return dbConnection;
// };

export default {};
